<h1>All Users</h1>

<ul id="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user_path(user), class: 'user_username' %>
    </li>
  <% end %>
</ul>

<%= link_to "Add new user", new_user_path, class: "new_user" %>
